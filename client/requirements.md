## Packages
framer-motion | Animations for list items and page transitions
lucide-react | Icons for UI elements
date-fns | Formatting dates in history

## Notes
The backend is Python/Flask, serving endpoints at /api/search and /api/history.
Authentication is not required for this simple search tool.
